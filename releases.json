/* The app component sets its own dark background; this just removes
   default margins and ensures full-height layout. */
html, body, #root {
  margin: 0;
  padding: 0;
  min-height: 100vh;
  background: #0B1622;
}
* { -webkit-font-smoothing: antialiased; }
